"""C1 module 5 — Le monde de la cour."""
from assemble import build_module
from spec_c1 import META
from rows_c1 import module_rows

MODULE, SEQUENCES, IMG_DIR = build_module(META[4], module_rows(4))
